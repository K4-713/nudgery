-- ED-17: per-question "One Yes Per Day" flag for YES/NO questions. Additive, defaults to off
-- (0 = sum every Yes, the prior behavior), so existing questions are unaffected.
ALTER TABLE Question ADD COLUMN collapse_per_day INTEGER NOT NULL DEFAULT 0;
